import SwiftUI

extension View {
    /// Modifier to ensure refresh control stays hidden (used in conjunction with global appearance setting)
    func hideRefreshControl() -> some View {
        self
    }
}
